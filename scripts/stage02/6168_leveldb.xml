<llpackages>
    <package name='leveldb' version='1.19' install='destdir'>
        <license>
            unknown
        </license>
        <sources>
            <file>
                <pkg sha1='864b45b4a8d1ad400b9115ff6d3c9fb1f79be82b'>
                    leveldb-1.19.tar.gz
                </pkg>
                <mirror>
                    https://github.com/google/leveldb/archive/
                </mirror>
            </file>
            <manualcheck date='20160929' interval='60' mirror='https://github.com/google/leveldb/archive/'/>
        </sources>
        <unpack>
            <![CDATA[tar xf ${SRCDIR}/leveldb-1.19.tar.gz
]]>
        </unpack>
        <patch/>
        <build>
            <![CDATA[cd leveldb-1.19
make -j ` grep -c processor /proc/cpuinfo ` 
]]>
        </build>
        <install>
            <![CDATA[cd leveldb-1.19
	    mkdir -p ${TGTDIR}/usr/lib
	    mkdir -p ${TGTDIR}/usr/include
	    cp --preserve=links out-shared/libleveldb.* ${TGTDIR}/usr/lib
	    cp --preserve=links out-static/libleveldb.* ${TGTDIR}/usr/lib
            cp -r include/leveldb ${TGTDIR}/usr/include/

]]>
        </install>
        <clean>
            <![CDATA[rm -rf leveldb-1.19
]]>
        </clean>
    </package>
</llpackages>