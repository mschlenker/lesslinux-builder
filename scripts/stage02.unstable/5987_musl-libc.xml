<llpackages>
	<package 
		name="musl"
		version="1.1.15"
		install="destdir"
		providedeps="no">
		<license>GPLv2</license>
		<sources>
			<file>                                                                                  
				<pkg sha1="027c3ae2182fa53c2b554ca98a28dc5cfca4b2c3">musl-1.1.15.tar.gz</pkg>
				<mirror>http://www.musl-libc.org/releases/</mirror>
			</file>
			<manualcheck date="20160701" interval="60" page="http://www.musl-libc.org/download.html" />
			<check>
				<page html="http://www.musl-libc.org/releases/">
					<atext linktext="musl-1.1.16." />
					<atext linktext="musl-1.1.17." />
					<atext linktext="musl-1.1.18." />
					<atext linktext="musl-1.1.19." />
					<atext linktext="musl-1.1.20." />
					<atext linktext="musl-1.2." />
					<atext linktext="musl-1.3." />
					<atext linktext="musl-2.0." />
				</page>
			</check>
		</sources>
		<builddeps>
                     <dep>cross-gcc-musl1</dep>
		     <dep>distcc</dep>
                </builddeps>
		<unpack>
			<![CDATA[ 
			tar xzf ${SRCDIR}/${PKGNAME}-${PKGVERSION}.tar.gz
			]]>
		</unpack>
		<build>
			<![CDATA[
			
cd ${PKGNAME}-${PKGVERSION}
for f in utmp paths ; do
        sed -i 's%/dev/null/wtmp%/var/log/wtmp%g' include/${f}.h
        sed -i 's%/dev/null/utmp%/var/run/utmp%g' include/${f}.h
done

./configure 
make 
exit 0

export PATH=/usr/local/crosstools-ia32-musl/bin:$PATH
export CLFS_HOST=$(echo ${MACHTYPE} | sed "s/-[^-]*/-cross/")
export CLFS_TARGET=i386-linux-musl
export CLFS_CPU=i686
			
CFLAGS=-fno-toplevel-reorder \
CC=${CLFS_TARGET}-gcc \
./configure \
  --prefix=/ \
  --target=${CLFS_TARGET}
CFLAGS=-fno-toplevel-reorder CC=${CLFS_TARGET}-gcc make

			]]>
		</build>
		<install>
			<![CDATA[
			
cd ${PKGNAME}-${PKGVERSION}
make install DESTDIR=${TGTDIR}
exit 0

export PATH=/usr/local/crosstools-ia32-musl/bin:$PATH
export CLFS_HOST=$(echo ${MACHTYPE} | sed "s/-[^-]*/-cross/")
export CLFS_TARGET=i386-linux-musl
export CLFS_CPU=i686

DESTDIR=${TGTDIR}/usr/local/crosstools-ia32-musl/${CLFS_TARGET} make install || exit 1
${CLFS}/cross-tools/bin/${CLFS_TARGET}-strip ${TGTDIR}/usr/local/crosstools-ia32-musl/${CLFS_TARGET}/lib/*.so*
${CLFS}/cross-tools/bin/${CLFS_TARGET}-strip ${TGTDIR}/usr/local/crosstools-ia32-musl/${CLFS_TARGET}/lib64/*.so*

			]]>
		</install>
		<clean>
			 <![CDATA[
			rm -rf ${PKGNAME}-${PKGVERSION}
			]]>
		</clean>
	</package>
</llpackages>
